# This file is used for convenience of local development.
# 'open set_env' to  open the file and see the variables
# 'source set_env' to use the env variables within this directory of Node server or working terminal
export POSTGRES_USERNAME=
export POSTGRES_PASSWORD=
export POSTGRES_HOST=
export POSTGRES_DATABASE=
export AWS_MEDIA_BUCKET=
export AWS_REGION=
export AWS_PROFILE=
export JWT_SECRET=


# We do not want your credentials to be stored in git. 
# After pulling this starter project, run the following command to tell git to stop tracking the script in git but keep it stored locally. 
# This way, you can use the script for your convenience and reduce risk of exposing your credentials.
# git rm --cached set_env.sh

